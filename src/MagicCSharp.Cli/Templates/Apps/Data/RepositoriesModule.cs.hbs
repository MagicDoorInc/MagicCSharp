using MagicCSharp.Data.Postgres;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;

namespace {{ prefix }}.{{ name }}.Data.EntityFramework;

public static class {{ name }}RepositoriesModule
{
    /// <summary>
    ///     Registers the context factory and every repository.
    ///     <para>
    ///         Repositories are registered explicitly rather than discovered, so the list of things that talk
    ///         to the database is readable in one place. AddEntity appends to it.
    ///     </para>
    /// </summary>
    public static IServiceCollection Add{{ name }}Repositories(this IServiceCollection services, IConfiguration configuration)
    {
        services.AddPostgresDbContextFactory<Magic{{ name }}Context>(configuration);

        return services;
    }
}
