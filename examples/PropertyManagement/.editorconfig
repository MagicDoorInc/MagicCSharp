root = true

# Entity Framework migrations are generated by `dotnet ef`, but only their Designer and snapshot files say so
# in a header. Without this the build rules would report on code nobody wrote.
[**/Migrations/*.cs]
generated_code = true
